# Optional backend URLs
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=

# Optional websocket URL (empty triggers reconnect banner)
REACT_APP_WS_URL=

# Mock toggle (defaults to mock=true when unset)
REACT_APP_USE_MOCK=1
